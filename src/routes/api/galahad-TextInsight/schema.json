{
  "type": "object",
  "properties": {
    "text": {
      "type": "string",
      "minLength": 50,
      "maxLength": 5000,
      "description": "The text to process"
    },
    "maxSummaryLength": {
      "type": "integer",
      "minimum": 50,
      "maximum": 500,
      "description": "Maximum length of the summary in words (default: 150)"
    },
    "maxKeyPhrases": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "description": "Maximum number of key phrases to extract (default: 5)"
    }
  },
  "required": ["text"],
  "additionalProperties": false
}